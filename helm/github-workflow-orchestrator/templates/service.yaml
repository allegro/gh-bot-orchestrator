apiVersion: v1
kind: Service
metadata:
  name: {{ include "gh-bot-orchestrator.fullname" . }}
  labels:
    {{- include "gh-bot-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gh-bot-orchestrator.selectorLabels" . | nindent 4 }}
