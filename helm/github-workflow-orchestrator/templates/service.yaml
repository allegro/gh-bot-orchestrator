apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-workflow-orchestrator.fullname" . }}
  labels:
    {{- include "github-workflow-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-workflow-orchestrator.selectorLabels" . | nindent 4 }}
